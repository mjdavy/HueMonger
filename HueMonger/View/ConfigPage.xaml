<Page
    x:Class="HueMonger.View.ConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HueMonger.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.DataContext>
        <Binding Path="Config" Source="{StaticResource Locator}"/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Command="{Binding ConfigureCommand}" HorizontalAlignment="Center">Configure HueMonger</Button>
            <TextBlock
                FontSize="36" 
                Visibility="{Binding MessageVisibility}" 
                Text="{Binding ConfigMessage}">
            </TextBlock>
        </StackPanel>
    </Grid>

</Page>
