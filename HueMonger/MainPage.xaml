<Page
    x:Class="HueMonger.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HueMonger"
    xmlns:vm="using:HueMonger.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" 
    Loaded="Page_Loaded"
    >

    <Page.Resources>
        <DataTemplate x:Key="HueLightDataItemTemplate" x:DataType="vm:LightViewModel">
            <StackPanel Width="400" Margin="12">
                <TextBlock Text="{Binding Name}" />
                <Slider
                    Maximum="100" 
                    Minimum="0"
                    Value="{Binding Brightness}"
                    />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Page.DataContext>
        <Binding Path="Main" Source="{StaticResource Locator}"/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!--<TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="72">Main View</TextBlock>-->
        <ListBox x:Name="LightSwitchList"
                    ItemTemplate="{StaticResource HueLightDataItemTemplate}"
                    ItemsSource="{Binding HueLights}" 
                    SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                    Margin="12,24,12,12" 
                 />

    </Grid>
</Page>
